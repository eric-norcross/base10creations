- if @sku
  = render "layouts/partials/breadcrumbs", product: @product, sku: @sku

.page{:id=>"#{@product.skin.name}"}
  %article
    = render "layouts/partials/gallery", items: @product.gallery_items(@sku), title: @product.title

    #product-details
      %header
        .hgroup
          %h1 
            sku ##{@sku.title}
          %h2
            = association(@product, @sku.finish)
          %h3
            = @product.title
        #icons
          = link_to "#", :"data-toggle" => "tooltip", title: "#{render 'layouts/partials/tooltip', product: @product}", class: "tt" do 
            = render '/layouts/svg/share'
          = link_to "#", class: "print" do 
            = render '/layouts/svg/print'

      = render "layouts/partials/product_tabs", product_object: @product, videos: @product.videos, feature_set: [@product], collection: @product.collection, categories: @product.categories, collection_id: @product.collection_id, finish_id: @sku.finish_id

    - unless @related.blank?
      = render "layouts/partials/product_footer", finish: @sku.finish
