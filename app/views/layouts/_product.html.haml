= form_tag("/products/2", method: "get") do
  .search-field
    = text_field_tag "#{id}", "Keyword or item #", {class: "clear-input", "data-message" => "Keyword or item #"}
    /= submit_tag "Submit"

