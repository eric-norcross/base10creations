%li
  .list-view-image
    = link_to product.path, { title: product.title } do
      = cl_image_tag(product.list_image, transformation: "list", alt: product.list_image, title: product.title)
  .list-view-info
    %h2.title
      = link_to product.title, product.path