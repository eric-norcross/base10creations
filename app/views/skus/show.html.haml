#admin
  %table
    %thead
      %tr
        %th Identifier:
        %th Finish:
        - if defined? @sku.images
          %th Images:
    %tbody
      %tr
        %td= @sku.identifier
        %td= @sku.finish
        - if defined? @sku.images
          - @sku.images.each do |asset|
            %td= image_tag asset.asset_url(:thumb).to_s

  .links
    = link_to 'Edit', edit_sku_path(@sku)
    \|
    = link_to 'Back', skus_path
