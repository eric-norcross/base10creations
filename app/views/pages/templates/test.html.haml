#admin
  %table
    %thead
      %tr
        %th Name:
        %th Title:
        %th Content:
        / %th Images:
        / %th Figures:
        / - if @page.figrues.length > 0
        /   %th Figure Image:
        / - if defined? @page.images
        /   - @page.images.each do
        /     %th Image:
    %tbody
      %tr
        %td= @page.name
        %td= @page.title
        %th= @page.content
        / %td= @page.figrue.title
        / - if @page.figrue.image.length > 0
        /   %td= image_tag @page.figrue.image[0].asset_url(:thumb).to_s
        / - if defined? @page.images
        /   - @page.images.each do |asset|
        /     %td= image_tag asset.asset_url(:thumb).to_s

  .links
    = link_to 'Edit', edit_sku_path(@page)
    \|
    = link_to 'Back', skus_path
