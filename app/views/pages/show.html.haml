.sections
  = render template: "/quotes/show", locals: { quote: page_presenter.random_quote } if page_presenter.random_quote.present?
  
  .section-group
    - page_presenter.grouped_sections.each do|section|
      = render template: "/sections/show", locals: { section: section }

  - page_presenter.work_sections.each do|section|
    = render template: "/sections/show", locals: { section: section }