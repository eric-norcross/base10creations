#admin
  %table
    %thead
      %tr
        %th Name:
        %th Title:
    %tbody
      %tr
        %td= @category.name
        %td= @category.title

  .links
    = link_to 'Edit', edit_category_path(@category)
    \|
    = link_to 'Back', categories_path
