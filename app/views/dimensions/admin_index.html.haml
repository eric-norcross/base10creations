#admin
  %h1 Available Dimension

  .links
    = link_to 'Add a new Dimension', new_dimension_path, {class: "button"}

  - if @dimensions
    %table
      %thead
        %tr
          %th Id:
          %th Title:
          %th Width:
          %th Height:
          %th Depth:
          %th Image:
          %th
      %tbody
        - @dimensions.each do |dimension|
          %tr
            %td= dimension.id
            %td= dimension.title
            %td= number_with_precision(dimension.width, strip_insignificant_zeros: true)
            %td= number_with_precision(dimension.height, strip_insignificant_zeros: true)
            %td= number_with_precision(dimension.depth, strip_insignificant_zeros: true)
            %td
              - if !dimension.images.blank?
                = cl_image_tag(dimension.images.first.asset.filename.to_s, transformation: "hint", alt: dimension.images.first.asset.filename.to_s, title: dimension.title)
              - else
                n/a
            %td.controls
              = link_to 'Show', dimension
              = link_to 'Edit', edit_dimension_path(dimension)
              = link_to 'Destroy', dimension, method: :delete, data: { confirm: 'Are you sure?' }

  .links
    = link_to 'Add a new Dimension', new_dimension_path, {class: "button"}
