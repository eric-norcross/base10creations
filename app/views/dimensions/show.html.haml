#admin
  %table
    %thead
      %tr
        %th Title:
        %th Width:
        %th Height:
        %th Depth:
        - if defined? @dimension.image
          %th Image:
    %tbody
      %tr
        %td= @dimension.title
        %td= @dimension.width
        %td= @dimension.height
        %td= @dimension.depth
        - if defined? @dimension.image
          %td
            - @dimension.image.each do |image|
              = cl_image_tag(image.asset.filename.to_s, transformation: "thumb", alt: image.asset.filename.to_s, title: @dimension.title)

  .links
    = link_to 'Edit', edit_dimension_path(@dimension)
    \|
    = link_to 'Back', dimensions_path
